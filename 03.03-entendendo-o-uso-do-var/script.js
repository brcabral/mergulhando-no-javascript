var age = 21

if (age > 18) {
    var test = 35
}

// É possível acessar a variável que foi definida dentro do if
// Pois as variáveis definidas com var é delimitada por função e não por blocos
console.log(test)
