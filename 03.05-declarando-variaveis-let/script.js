/**
 * O let é usado para declarar variáveis, porém diferente do var
 *   não é possível reatribuir o valor da variável (let)
 * Diferente do var o let é delimitado por bloco.
 */

let age = 21
// let age = 27

if (age > 18) {
    let age = 32
    console.log(age)
}
